# Puzzoo CCR Platform — Streamlit

Native Streamlit rebuild of `puzzoo-ccr-platform.html`.

## Run locally

```bash
pip install -r requirements.txt
streamlit run app.py
```

The original HTML content and positioning were used as the source for the Streamlit implementation.
